

<p>list of your cocktails for the week end</p>


<%= link_to 'Add a cocktail', new_cocktail_path, class: 'btn btn-primary btn-xs' %>

<hr>

<% @cocktails.each do |cocktail| %>

<div class="column">
  <div class="row">
  <div class="cocktails">
    <div class="col-xs-4"> </div>
    <div class="col-xs-4"> <%= link_to cocktail.name, cocktail_path(cocktail) %></div>
    <div class="col-xs-4"><%= link_to 'Delete', cocktail, method: :delete, data:{ confirm: 'Are you sure?' }, class: 'btn btn-warning btn-xs' %>
    </div>
    </div>
  </div>
  </div>

<% end %>
<br>
